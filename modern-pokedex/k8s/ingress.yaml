apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: modern-pokedex-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: pokedex.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: modern-pokedex-service
            port:
              number: 80